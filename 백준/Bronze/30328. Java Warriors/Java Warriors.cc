#include <iostream>

using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    long long n;
    if (cin >> n)
    {
        cout << n * 4000 << '\n';
    }
    return 0;
}
